role:
You are a senior full-stack engineer, security specialist, and UX systems architect working inside the existing Vite + React codebase of "subnation". You are responsible for implementing validation rules, anti-abuse mechanisms, authentication expansion, and overall system refinement without breaking existing functionality.

task:
Enhance the platform by:

* Enforcing strict phone number validation rules
* Adding OAuth login methods (Google + Telegram)
* Implementing anti-abuse limits on pending wallet recharge requests
* Improving overall system security, structure, and UI consistency

context:

* Platform is already stable and advanced
* Core systems (wallet, referrals, admin, notifications) are implemented
* Current need:

  * Prevent abuse and fake data
  * Improve login flexibility
  * Add system-level protections
  * Continue polishing UX and UI

reasoning:

* Enforce data integrity at both frontend and backend levels
* Prevent exploitation of wallet recharge system
* Improve accessibility via social login
* Strengthen system reliability and structure
* Maintain mobile-first and performance standards

stop conditions:

* Phone inputs accept only valid Libyan prefixes
* OAuth login (Google + Telegram) fully working
* Pending recharge limit enforced correctly
* No abuse scenarios possible
* UI remains clean and consistent
* No regression or performance issues introduced

output:

1. Phone Number Validation:

   * Enforce allowed prefixes only:

     * 091 / 093 (Libyana)
     * 092 / 094 (Almadar)

   * Rules:

     * Must be numeric
     * Must start with one of the allowed prefixes
     * Fixed length validation (Libyan format)

   * Apply validation in:

     * Registration
     * Wallet recharge forms
     * Profile updates

   * UX:

     * Clear error messages
     * Prevent invalid submission

2. OAuth Authentication:

   * Add login/signup via:

     * Google (OAuth 2.0)
     * Telegram (login widget/API)

   * Requirements:

     * Link to existing accounts if email/phone matches
     * Prevent duplicate accounts
     * Secure token handling

   * UI:

     * Clean buttons on login/register pages
     * Mobile-friendly layout

3. Pending Recharge Limit (Anti-Abuse):

   * Define rule:

     * Max pending recharge requests per user = 3

   * Behavior:

     * If user reaches limit:

       * Block new recharge submissions
       * Show clear message:
         "لديك طلبات قيد المراجعة، يرجى الانتظار حتى يتم اعتمادها"

   * Backend enforcement:

     * Validate before creating new request
     * Prevent bypass

   * Admin:

     * Can approve / reject to free slots

4. Security Enhancements:

   * Sanitize all inputs (frontend + backend)
   * Protect endpoints from abuse
   * Validate all critical operations server-side
   * Prevent duplicate submissions (debounce / request lock)

5. UX & UI Refinement:

   * Improve forms:

     * Clear labels
     * Better spacing
     * Inline validation
   * Maintain design consistency
   * Ensure smooth mobile experience

6. System Stability:

   * Ensure:

     * No conflicts with existing features
     * No broken flows
     * Clean integration with current architecture

7. Final Validation:

   * Test:

     * Phone validation (all cases)
     * OAuth login flows
     * Recharge limit enforcement
   * Ensure:

     * Accurate behavior
     * Clear UX
     * No loopholes

Implement cleanly and securely within the current system architecture.
