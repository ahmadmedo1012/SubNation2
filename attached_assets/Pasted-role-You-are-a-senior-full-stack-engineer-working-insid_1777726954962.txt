role:
You are a senior full-stack engineer working inside the existing Vite + React codebase of "subnation". You are implementing Phase 3 (Wallet System Enhancement) with full awareness of the current architecture and previous phases.

task:
Enhance and restructure the wallet recharge system with improved UX, clearer flows, and support for multiple payment methods (mobile balance + bank transfer via LyPay). Focus on simplicity, clarity, and mobile-first usability.

context:

* Platform is already stable and modern (Vite + React)
* Current wallet system exists but needs UX and functional improvements
* الهدف: جعل عملية الشحن واضحة، سهلة، ومنظمة بدون تعقيد

reasoning:

* Improve current recharge flow instead of replacing it
* Reduce user confusion during payment
* Add structured input requirements to avoid errors
* Use dynamic UI based on selected payment method/network
* Ensure everything is mobile-friendly and fast

stop conditions:

* Wallet recharge flow is clear and easy to complete
* All required data is collected correctly
* Dynamic amount buttons work based on selected network
* LyPay method fully implemented with clear instructions
* No UX confusion or missing inputs
* Fully working without bugs

output:

1. Mobile Balance Recharge (تحويل رصيد):

   * Update recharge form to include:

     * Network selection (Libyana / Almadar)
     * Amount input
     * Sender phone number (required)
     * Screenshot upload (optional)

   * Dynamic preset buttons based on network:

     * If Libyana:

       * Show buttons: 1 / 5 / 10 / 20 / 50
     * If Almadar:

       * Show buttons: 1 / 5 / 10 / 20
     * Clicking a button auto-fills the amount field

   * UX:

     * Clear step-by-step layout:

       1. Choose network
       2. Choose or enter amount
       3. Enter sender number
       4. Upload screenshot (optional)
       5. Submit

2. LyPay Bank Transfer Method:

   * Add new recharge method: "تحويل مصرفي (LyPay)"

   * Display static account info:

     * Account name
     * IBAN
     * Bank name
     * Branch

   * Add copy buttons:

     * Copy IBAN
     * Copy account number
     * Copy full details

   * User input fields:

     * Amount (required)
     * Sender bank account number (required)
     * Screenshot upload (required)

   * UX:

     * Clear instructions:

       * Transfer first
       * Then fill the form
     * Clean layout for mobile

3. Form Validation:

   * Ensure:

     * Required fields cannot be skipped
     * Proper validation messages
     * Prevent submission if incomplete

4. UI/UX Improvements:

   * Clean spacing and grouping
   * Clear labels in Arabic
   * Highlight important fields
   * Responsive and mobile-first layout

5. Admin Side (if applicable):

   * Display submitted data clearly:

     * Network / method
     * Amount
     * Sender info
     * Screenshot
   * Easy verification workflow

6. Final Validation:

   * Test both methods:

     * Libyana recharge
     * Almadar recharge
     * LyPay transfer
   * Ensure:

     * Data saved correctly
     * UI clear and smooth
     * No bugs or missing steps

Implement cleanly within current system without breaking existing functionality.
