role:
You are a senior full-stack architect, migration specialist, and elite frontend engineer operating inside Replit with full access to the provided website and codebase. You are responsible for auditing, stabilizing, and fully transforming the platform into a modern Vite + React application with production-grade quality.

task:

1. Access and analyze the live website:

   * https://subnation.free.nf/
   * https://subnation.free.nf/subnation_vault.php
   * Admin access:

     * username: admin
     * password: SubNation@2026

2. Perform a complete, deep audit of:

   * Frontend (UI/UX, structure, responsiveness)
   * Backend (PHP logic, API patterns, database usage)
   * Full user flows (auth, subscriptions, checkout, wallet, admin)

3. Then execute a full migration:

   * Convert the platform from PHP/MySQL (monolithic) → modern architecture:

     * Frontend: Vite + React (clean, modular, mobile-first)
     * Backend: keep PHP temporarily OR refactor into clean API layer

4. Deliver a production-ready, high-performance, clean, scalable system.

context:

* Platform: digital subscriptions system
* Current issues:

  * Monolithic PHP structure
  * UI inconsistency
  * UX friction
  * Weak architecture separation
  * Not optimized for mobile or performance
* Target:

  * Clean architecture (frontend separated from backend)
  * Modern UI (premium, consistent, minimal)
  * Fast performance (especially mobile)
  * Maintainable and scalable codebase

reasoning:

* Phase 1: Deep Audit

  * Map all pages, endpoints, flows
  * Identify logic, database interactions
  * Detect weaknesses (UX, performance, structure)
* Phase 2: Architecture Design

  * Define:

    * React app structure (Vite)
    * Routing system
    * State management
    * API layer (REST or equivalent)
* Phase 3: Incremental Migration

  * Start with frontend rebuild:

    * Pages → React components
    * UI redesign (clean, consistent, mobile-first)
  * Gradually decouple PHP into API endpoints
* Phase 4: Optimization

  * Remove duplication
  * Improve performance
  * Ensure smooth UX flows
* Phase 5: Validation

  * Test all flows end-to-end

stop conditions:

* Full platform migrated to Vite + React (frontend)
* All original features preserved and improved:

  * Auth
  * Subscriptions
  * Checkout
  * Wallet
  * Admin
* UI is clean, consistent, and premium-level
* Mobile experience is smooth and fully optimized
* Backend is structured as a clean API (even if still PHP)
* No broken flows, no inconsistencies, no performance issues
* Codebase is modular, clean, and maintainable

output:

1. Full Audit Report:

   * Structure
   * Flows
   * Issues (UX / performance / architecture)

2. New Architecture:

   * React (Vite) folder structure
   * Routing system
   * State management
   * API design

3. Migration Execution:

   * Rebuilt frontend (React components)
   * Clean UI system
   * Mobile-first layout

4. Backend Refactor:

   * Organized API endpoints
   * Improved structure (controllers/services if applicable)

5. UI/UX Upgrade:

   * Clean design system
   * Consistent components
   * Improved flows (subscriptions / checkout / wallet)

6. Performance Optimization:

   * Faster loading
   * Lightweight assets
   * Smooth rendering

7. Final Validation:

   * Test all user and admin flows
   * Ensure stability and production readiness

Execute with zero shortcuts. Deliver a clean, fast, modern, and professional platform at the highest possible level.
