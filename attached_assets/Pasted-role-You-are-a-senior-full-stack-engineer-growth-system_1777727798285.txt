role:
You are a senior full-stack engineer, growth systems designer, and UX optimization expert working inside the existing Vite + React codebase of "subnation". You are implementing Phase 4 (Loyalty System + UX Enhancement + Support System) with full awareness of previous phases and current architecture.

task:
Enhance the platform by implementing a structured loyalty/referral system, improving UI/visual quality, adding analytics (charts), and building an integrated support/ticket system — all without breaking existing functionality.

context:

* Platform is stable and modern (Vite + React)
* Previous phases completed (core system + wallet + enhancements)
* This phase focuses on:

  * Growth (referrals)
  * Visual polish
  * Admin insights
  * Reducing manual customer support load

reasoning:

* Build a fraud-resistant referral system
* Encourage users to share referral links
* Keep UX simple and motivating
* Add visual analytics for better insights
* Replace WhatsApp support with structured in-app system
* Maintain performance and mobile-first design

stop conditions:

* Referral system fully functional and protected from abuse
* Users can بسهولة مشاركة رابط/كود الإحالة
* Points system works accurately with clear conversion
* Charts visible and accurate (admin + optionally user view)
* UI improved with better visuals and effects
* Support system fully replaces external messaging
* No bugs, no inconsistencies, no performance issues

output:

1. Loyalty & Referral System:

   * Generate unique referral code per user

   * Provide:

     * Referral link (copy button)
     * Referral code (copy button)

   * Reward logic:

     * Each valid referral = 50 points
     * Every 100 points = 1 LYD

   * Conditions (anti-fraud):

     * Referral counted ONLY after:

       * New user registers via referral
       * AND completes wallet recharge (confirmed)
     * Prevent:

       * Self-referral
       * Multiple accounts abuse (basic checks: IP/device/email patterns)

   * UI:

     * Dedicated “Referral / Loyalty” page
     * Show:

       * Total points
       * Equivalent balance
       * Number of successful referrals
     * CTA:

       * “Invite friends” with copy/share buttons

2. Points Conversion System:

   * Allow user to convert points → wallet balance
   * Validation:

     * Minimum threshold (e.g. 100 points)
   * Instant or queued conversion (based on system design)

3. UI/UX & Visual Enhancement:

   * Improve:

     * Colors consistency
     * Shadows, spacing, alignment
     * Button styles (hover / active / focus)
   * Add subtle animations:

     * Hover effects
     * Transitions
     * Loading states
   * Ensure premium, clean, modern look

4. Charts & Analytics:

   * Add charts (using lightweight library):

     * Admin dashboard:

       * Total users
       * Total sales
       * Orders over time
     * Optional (main site or user dashboard):

       * Total subscriptions sold
       * Activity indicators

   * Requirements:

     * Real-time or near real-time updates
     * Clean, readable charts
     * Mobile-friendly rendering

5. Support / Ticket System:

   * Replace WhatsApp with in-app support

   * User side:

     * “Support / Help” section
     * Create ticket:

       * Title
       * Message
       * Category (optional)
     * View replies and status

   * Admin side:

     * Full ticket management:

       * View all tickets
       * Reply
       * Change status (open / closed)
     * Organized UI (table + filters)

   * Optional:

     * Notification when reply is added

6. Admin Improvements:

   * Add:

     * Referral tracking per user
     * Points overview
     * Simple analytics panels

7. Validation:

   * Test:

     * Referral flow end-to-end
     * Points calculation and conversion
     * Charts accuracy
     * Support system (create/reply/close)
   * Ensure:

     * No abuse possible
     * UI clear and smooth
     * Performance unaffected

Implement everything cleanly, modularly, and without breaking existing features. Focus on clarity, performance, and professionalism.
