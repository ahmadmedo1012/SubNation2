role:
You are a senior full-stack architect, authentication systems engineer, and production-readiness specialist working directly inside the "subnation" project (Vite + React + backend API + Firebase integration). You are responsible for preparing the platform for advanced Firebase Authentication support and improving overall production quality.

task:
Enhance and prepare the platform to support all major Firebase Authentication providers in a scalable and maintainable way, while also creating an admin-based credential/configuration management system so sensitive settings can be managed from the dashboard instead of editing files manually.

context:

* Platform is already stable, modern, and visually polished
* Firebase integration already exists partially
* Goal:

  * Make authentication system extensible and future-proof
  * Simplify management of credentials/settings
  * Improve deployment readiness and professionalism
* Current pain point:

  * Editing credentials/configs directly in files is inconvenient and risky

reasoning:

* Do NOT hardcode Firebase/Auth configs
* Build centralized configuration management
* Prepare authentication architecture for:

  * Google
  * Telegram
  * Email/password
  * Phone auth
  * GitHub
  * Facebook
  * Apple
  * Any provider supported by Firebase Authentication
* Ensure:

  * Modular provider system
  * Secure credential storage
  * Easy enable/disable from admin dashboard

stop conditions:

* Firebase Authentication system is modular and extensible
* Admin can manage auth credentials/settings from dashboard
* Providers can be enabled/disabled easily
* No sensitive config editing required in code/files
* Authentication system is production-ready
* UI and UX remain clean and professional

output:

1. Authentication System Refactor:

   * Refactor auth system into modular provider-based architecture
   * Create centralized auth service layer
   * Prepare support for:

     * Google
     * Telegram
     * Email/password
     * Phone authentication
     * GitHub
     * Facebook
     * Apple
     * Future providers

2. Firebase Integration Cleanup:

   * Standardize Firebase initialization
   * Remove duplicated auth logic
   * Ensure:

     * Proper session persistence
     * Clean token handling
     * Secure provider flow

3. Admin Credential Management Panel:

   * Create secure admin page for managing:

     * Firebase config
     * OAuth credentials
     * API keys
     * Redirect URLs
     * Provider enable/disable toggles

   * Features:

     * Save/update credentials from dashboard
     * Validation before saving
     * Mask sensitive values
     * Optional test connection button

4. Provider Management System:

   * Admin can:

     * Enable/disable providers individually
     * Configure login methods visually
   * Frontend automatically updates visible login buttons

5. Authentication UX Improvements:

   * Improve login/register screens:

     * Cleaner layout
     * Better provider buttons
     * Consistent spacing/icons
   * Mobile-first optimization

6. Security:

   * Never expose secrets unnecessarily
   * Encrypt/store sensitive credentials securely if needed
   * Validate provider configs before activation

7. Production Readiness:

   * Ensure auth works correctly in:

     * Local
     * Vercel
     * Future hosting environments
   * Remove environment inconsistencies

8. Codebase Refinement:

   * Clean auth-related files
   * Remove legacy login logic
   * Improve maintainability

9. Final Validation:

   * Test:

     * All enabled providers
     * Session persistence
     * Logout/login flow
     * Admin credential editing
   * Ensure:

     * No broken flows
     * Stable authentication
     * Clean UX

Implement everything cleanly and professionally so the platform becomes scalable, easy to manage, and fully ready for production deployment.
