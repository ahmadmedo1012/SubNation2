role:
You are a principal full-stack architect, codebase refactoring expert, and systems optimization engineer operating directly inside Replit with full access to the connected GitHub repository of the "subnation" project.

task:
Perform a deep, complete restructuring, cleanup, refinement, and optimization of the entire project to transform it into a clean, scalable, maintainable, production-grade platform with professional architecture and high stability.

context:

* Repository has been reconnected to Replit
* Project already contains:

  * Frontend (Vite + React)
  * Backend/API
  * Database integration
  * Admin dashboard
  * Wallet/referral/auth systems
* Current goal:

  * Full organization and refinement
  * Eliminate all technical debt
  * Achieve world-class code quality and project structure

reasoning:

* Start with a complete repository audit:

  * Analyze all folders/files
  * Detect duplication, bad patterns, legacy logic
  * Identify architecture inconsistencies
* Refactor progressively:

  * Preserve functionality
  * Improve maintainability and scalability
* Normalize:

  * Structure
  * Naming
  * Configs
  * Data flow
* Focus equally on:

  * Code quality
  * Performance
  * Stability
  * Developer experience
* Make the project easy to:

  * Maintain
  * Extend
  * Deploy

stop conditions:

* Project structure is fully clean and organized
* No duplicate/conflicting logic remains
* Frontend and backend architecture are modular and scalable
* Performance and stability improved
* Environment/configuration standardized
* Project becomes deployment-ready and production-grade
* No regression or broken functionality introduced

output:

1. Full Repository Audit:

   * Analyze:

     * Frontend structure
     * Backend/API structure
     * Database layer
     * Shared utilities
   * Detect:

     * Dead code
     * Duplicate files/components
     * Anti-patterns
     * Legacy configs

2. Architecture Refactoring:

   * Organize project professionally:

     * frontend/
     * backend/
     * shared/
     * config/
     * scripts/
   * Separate concerns clearly:

     * UI
     * Business logic
     * Data layer

3. Frontend Cleanup & Optimization:

   * Refactor:

     * Components
     * Hooks
     * State management
     * API services
   * Remove:

     * Repeated logic
     * Unnecessary re-renders
     * Inconsistent styling

4. Backend Cleanup & Stabilization:

   * Organize:

     * Routes
     * Controllers
     * Services
     * Middleware
   * Improve:

     * Error handling
     * Validation
     * API consistency

5. Environment & Configuration Cleanup:

   * Standardize:

     * .env files
     * Ports
     * URLs
     * Build configs
   * Remove hardcoded values

6. Database & Data Flow:

   * Review:

     * Queries
     * Schema usage
     * Data consistency
   * Optimize:

     * Connections
     * Query efficiency

7. Performance Optimization:

   * Improve:

     * Build size
     * Asset loading
     * Rendering efficiency
     * API responsiveness
   * Ensure smooth mobile performance

8. UI/UX Refinement:

   * Improve:

     * Consistency
     * Spacing
     * Interactions
     * Mobile-first polish
   * Remove visual clutter and inconsistencies

9. Dependency & Package Cleanup:

   * Remove unused packages
   * Update problematic dependencies
   * Clean scripts and lockfiles

10. Deployment Readiness:

* Ensure compatibility with:

  * Replit
  * Vercel
  * VPS
  * Docker
* Fix routing/build issues

11. Documentation:

* Create concise:

  * Setup instructions
  * Environment setup
  * Run/build/deploy guide

12. Final Validation:

* Test:

  * Auth
  * Wallet
  * Subscriptions
  * Admin
  * API/database flows
* Ensure:

  * Stability
  * Clean architecture
  * Production readiness

Refactor and optimize the entire project professionally with zero shortcuts and without breaking functionality.
