role:
You are a senior full-stack engineer, real-time systems architect, and UX optimization specialist operating inside Replit on the existing Vite + React version of the "subnation" platform. You are working on Phase 2 (Enhancement & Advanced Features), not a rebuild.

task:
Extend and enhance the current platform by implementing advanced UI/UX improvements, real-time capabilities, authentication upgrades, and admin system enhancements while preserving the existing architecture and stability.

context:

* Platform already migrated to Vite + React (Phase 1 completed)
* Current version is:

  * Fast
  * Clean
  * Functional
* This phase focuses on:

  * UX refinement
  * Feature expansion
  * Real-time systems
  * Admin power tools

DO NOT rebuild from scratch. Build on top of the current system.

reasoning:

* Maintain current architecture and improve it incrementally
* Add features in a modular, scalable way
* Ensure no regression or performance degradation
* Prioritize:

  * Real-time experience
  * Smooth UI interactions
  * Mobile-first consistency
* All new systems must integrate cleanly with existing codebase

stop conditions:

* Dark/light theme fully implemented without visual inconsistencies
* Typography and visuals are crisp, modern, and consistent
* OAuth login (Google + Telegram) fully functional
* Admin dashboard is real-time (no full page reloads)
* Telegram notifications system is working
* Real-time in-app notifications implemented
* Admin has full control over users
* No bugs, no UX inconsistencies, no performance regressions

output:

1. Theme System (Dark/Light):

   * Implement full theme switching:

     * Global theme provider (React context)
     * CSS variables or design tokens
     * Persist user preference (localStorage or backend)
   * Ensure:

     * All components support both themes
     * No contrast or readability issues
     * Smooth transition (no flicker)

2. UI/Visual Refinement:

   * Improve typography:

     * Modern font selection
     * Clear hierarchy
     * Better readability on mobile
   * Enhance visuals:

     * Subscription images (high quality, consistent ratio)
     * Button states (hover, active, disabled)
     * Shadows, spacing, and alignment
   * Ensure pixel-perfect consistency

3. Authentication Expansion:

   * Add OAuth login:

     * Google (OAuth 2.0)
     * Telegram login widget/API
   * Link social accounts to user profiles
   * Handle edge cases:

     * Existing users
     * Duplicate accounts

4. Real-Time Admin Dashboard:

   * Convert dashboard to SPA behavior:

     * No full reloads between pages
   * Implement real-time data updates using:

     * WebSocket OR polling (efficient)
   * Live updates for:

     * Users
     * Orders
     * Transactions

5. Telegram Integration:

   * Create Telegram bot integration:

     * Send notifications for:

       * New user registrations
       * Wallet recharge
       * Purchases
   * Configurable from admin panel

6. User Management System:

   * Admin can:

     * View all users
     * Edit user data
     * Adjust balances
     * Activate/deactivate accounts
   * Clean UI for management (tables + actions)

7. Real-Time Notifications System:

   * In-app notifications:

     * Live alerts (no refresh)
     * Categorized (orders / wallet / system)
   * UI:

     * Notification bell
     * Dropdown panel
   * Backend:

     * Event-based system

8. Performance & Stability:

   * Ensure all new features:

     * Do not slow down the app
     * Are optimized for mobile
     * Are modular and maintainable

9. Validation:

   * Test:

     * Theme switching
     * OAuth login
     * Real-time updates
     * Notifications
     * Admin actions
   * Ensure:

     * No UI breaks
     * No inconsistent states
     * No performance drops

Implement everything incrementally, cleanly, and without breaking existing functionality. Maintain production-level quality.
